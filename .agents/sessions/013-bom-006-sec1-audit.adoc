= Session 013 — BOM 0.0.6 publié + SEC-1 audit statique + dynamique
:session: 013
:date: 2026-06-30
:type: publication + sécurité
:score: 9/10

== Objectif

Publier BOM 0.0.6 sur Maven Central + EPIC SEC-1 (traquer tests coupables
qui font des appels LLM réseau).

== Ce qui a été fait

=== 1. BOM 0.0.6 publié sur Maven Central

- `./gradlew publishAggregationToCentralPortal --no-daemon -x check -x runAllTests`
- Bundle `3b886b28-c327-4f95-9b34-7c036544ed05` uploadé + publié (AUTOMATIC)
- Vérifié sur `repo1.maven.org` → HTTP 200
- Toutes les dépendances (contracts 0.0.2 + plugins N2) vérifiées sur Central avant publication

=== 2. SEC-1 US-SEC-1.1 — Audit statique + dynamique

*Premier audit (statique uniquement)* : déclaré "0 coupable" à tort.

*Deuxième audit (dynamique)* : analyse des GradleRunner qui exécutent des tâches
LLM sans mock. **1 borough coupable identifié** :

- **planner-gradle** — 6 tests `DecomposeIntentionPluginTest` + `DecomposeIntentionMultiCanalTest`
  exécutent `generatePlan` via GradleRunner sans mock. `IntentionPlanner.plan()` appelle
  `model.chat()` → appel réseau réel. Avant purge : `deepseek-v4-pro:cloud` sur `11434`.
  Après purge : `gemma4:31b-cloud` sur `11437` — toujours un appel réseau non protégé.

*Origine des 8 appels DeepSeek (session 012)* : les defaults du code `main` des plugins,
déclenchés par les tests fonctionnels GradleRunner qui exécutent les tâches Gradle.

=== 3. SEC-1 US-SEC-1.2 — Tests coupables corrigés

- 6 tests planner-gradle : ajout `-PollamaBaseUrl=http://localhost:1` (port fermé)

=== 4. Purge deepseek + ports 11434-11436 (cross-boroughs)

- planner-gradle : CI decompose.yml (deepseek → gpt-oss, 11434 → 11444)
- plantuml-gradle : application.yaml + 5 configs (11434 → 11438/11440)
- codebase-gradle : 3 features + CI anonymize.yml (deepseek → gpt-oss, 11434 → 11440)
- training-gradle : 3 fichiers main + 1 test (deepseek → gemma4, 11434 → 11442)
- workspace-gradle : provision-plugin + 3 tests pilot (deepseek → gpt-oss)
- container-provision : notebook Colab (BOM 0.0.5 → 0.0.6, 11434 → 11440)

=== 5. Notebook Colab mis à jour

- `start-ollama-tunnel.ipynb` : BOM 0.0.6 + port 11440

== Fichiers modifiés (workspace-bom)

* `.agents/INDEX.adoc` — session 013 ajoutée
* `BACKLOG.adoc` — SEC-1 US-SEC-1.1/1.2/1.3 marqués TERMINÉ
* `PROMPT_REPRISE.adoc` — session 014

== Boroughs altérés (sessions clôturées)

* planner-gradle (055), plantuml-gradle (154), codebase-gradle (170)
* training-gradle (067), workspace-gradle (009), container-provision (003)

== Prochaines étapes

* Maintenance / backlog
* Vérifier `runAllTests` BUILD SUCCESSFUL
* Capsule-plugin et slider-plugin non publiés sur Central (artifactId mismatch BOM)