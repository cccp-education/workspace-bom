= Session 017 — PUB-3.06 pipeline-contracts + BOM 0.0.7 + DOC-8.1 vérification
:code-name: MEMPHIS
:date: 2026-07-05
:type: ops + dev
:score: 9/10

== Objectif

Publication Maven Central de `pipeline-contracts:0.0.2` (PUB-3.06) +
Bump BOM `0.0.6 → 0.0.7` + publication + vérification du câblage DOC-8.1
dans document-gradle.

== Réalisations

=== Priorité 1 — PUB-3.06 pipeline-contracts ✅
. `cd pipeline-contracts && ./gradlew publishAggregationToCentralPortal --no-daemon`
. Bundle `f235a1da-7ff3-42cb-8b5b-5b91fb28b4af` uploadé + publishing.
. Vérifié sur Maven Central : `https://repo1.maven.org/maven2/education/cccp/pipeline-contracts/0.0.2/` → HTTP 200.
. `BACKLOG.adoc` PUB-3.06 → ✅ PUBLIÉ + VÉRIFIÉ.

=== Priorité 2 — DOC-8.1 câblage document-gradle ✅ (vérification)
Le câblage a été implémenté en session 009 (New Orleans). Cette session
vérifie l'état vert et nettoie le TODO obsolète.

. **Code main** (5 fichiers dans `document/releasenotes/` + `ReleaseNotesGenerateTask.kt`) :
  - `ConventionalCommitParser` — parse `feat(scope): message` (type, scope, message).
  - `CliGitLogParser` — git CLI (`git log --format`), `detectFromTag`, `detectVersion` (VERSION file → gradle.properties → SNAPSHOT).
  - `AsciidocReleaseNotesRenderer` — AsciiDoc `= Release X.Y.Z` + sections par catégorie + scope entre parenthèses.
  - `GitReleaseNotesGenerator` — compose parser + renderer, écrit `build/release-notes/release-notes-{version}.adoc`.
  - `ReleaseNotesGenerateTask` — task Gradle `releaseNotesGenerate` (group `document`), inputs fromTag/toTag/version/includeDownloads, outputDir.
. **Tests unitaires** (29/29 PASS) :
  - `ConventionalCommitParserTest` — 8 tests (parse, types, scope, breaking change, empty, non-conventional).
  - `CliGitLogParserTest` — 8 tests (parse, detectFromTag, detectVersion VERSION/gradle.properties/SNAPSHOT).
  - `AsciidocReleaseNotesRendererTest` — 7 tests (title with version, groups by category, scope, empty categories, downloads, empty commits, renderToFile).
  - `GitReleaseNotesGeneratorTest` — 5 tests (config version, SNAPSHOT, outputDir, fromTag detection).
. **Tests fonctionnels** (4/4 PASS — `ReleaseNotesFunctionalTest`) :
  - task registered, produces adoc from conventional commits, SNAPSHOT empty repo, reads VERSION file.
. **Cucumber BDD** (3/3 scénarios `@doc8` PASS) :
  - `release-notes.feature` — task registration, AsciiDoc from conventional commits, SNAPSHOT filename.
. **Nettoyage** : `DocumentI18nBridge.kt` — TODO "pipeline-contracts pas encore publie" remplacé par note "publié session 017".

*Note* : Le task Gradle `cucumberTest` échoue sur un bug Gradle 9.6.1
(`java.io.EOFException` dans `getPreviousFailedTestClasses` — fichiers
`output-events.bin` et `results-generic.bin` vides). Les scénarios
eux-mêmes PASSent tous. Bug contourné avec `--rerun-tasks` pour
vérifier le vert, mais le task remonte quand même l'EOFException.
Aucune régression fonctionnelle — c'est un problème d'infrastructure
Gradle 9.6.1, pas du code.

=== Priorité 3 — BOM 0.0.7 ✅
. `build.gradle.kts` racine : `version = "0.0.6"` → `version = "0.0.7"`.
. Constraint `pipeline-contracts:0.0.2` déjà présente (ligne 105, ajoutée session 016).
. `./gradlew publishAggregationToCentralPortal --no-daemon` — bundle `ff4ead73-1ea3-4bda-95ae-7a09d00127ef`.
. Vérifié sur Maven Central : `https://repo1.maven.org/maven2/education/cccp/workspace-bom/0.0.7/` → HTTP 200.

== Méthode baby-step DDD/TDD/BDD

La session 009 (New Orleans) a implémenté DOC-8.1 en baby-step :
- **Unit** d'abord (ConventionalCommitParser, CliGitLogParser, AsciidocReleaseNotesRenderer, GitReleaseNotesGenerator) — TDD test-first.
- **Functional** ensuite (ReleaseNotesFunctionalTest avec GradleRunner).
- **Cucumber BDD** enfin (release-notes.feature, 3 scénarios @doc8).

Cette session 017 vérifie que tout est vert et ferme la boucle publication.

== Vérifications

. `pipeline-contracts:0.0.2` sur Maven Central : HTTP 200 ✅
. `workspace-bom:0.0.7` sur Maven Central : HTTP 200 ✅
. document-gradle `./gradlew test` : 29/29 PASS ✅
. document-gradle `./gradlew functionalTest` : 14/14 PASS ✅
. document-gradle `./gradlew cucumberTest` : 3/3 scénarios @doc8 PASS (task EOFException bug Gradle 9.6.1) ✅

== Prochaine session

Session 018 — Mise à jour des consommateurs BOM 0.0.7 (document-gradle
`workspace-bom:0.0.6` → `0.0.7` dans libs.versions.toml + build.gradle.kts).
Vérification que toutes les contraintes N0 sont alignées sur 0.0.2.

== Fichiers modifiés

. `BACKLOG.adoc` — PUB-3.06 → ✅ PUBLIÉ + VÉRIFIÉ.
. `build.gradle.kts` — version `0.0.6 → 0.0.7`.
. `foundry/public/document-gradle/document-plugin/src/main/kotlin/document/DocumentI18nBridge.kt` — TODO obsolète nettoyé.

== Artefacts publiés

. `education.cccp:pipeline-contracts:0.0.2` — bundle `f235a1da-7ff3-42cb-8b5b-5b91fb28b4af`.
. `education.cccp:workspace-bom:0.0.7` — bundle `ff4ead73-1ea3-4bda-95ae-7a09d00127ef`.