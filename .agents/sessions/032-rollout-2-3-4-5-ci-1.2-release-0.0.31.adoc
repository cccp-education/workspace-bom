= Session 032 — EPIC MEM-CAT-ROLLOUT : ROLLOUT-2/3/4/5 + US-CI-1.2 + release 0.0.31 (4 boroughs)

_Date_ : 2026-09-02
_Type_ : cross-borough + pub (5 repos modifiés, 1 release Central)
_Mission_ : suite et fin du plan transverse S-028. ROLLOUT-1 document avait
été livré en session dédiée (S-244).

== ROLLOUT-2 codex-gradle (S-211)

Baby-step TDD strict RED → GREEN. Garde `CodexPluginPublicationTest` 3/3
(pattern Document S-244). Pin ws 0.0.30 + `version = ws.versions.codex.plugin.get()`
(D3 — remplace dérivation toml local, valeur inchangée 0.0.6) + platform
0.0.26→0.0.30. Particularité : l'id `education.cccp.codex` est déclaré inline
dans le block `gradlePlugin` (pas le toml) — parsing de garde corrigé en
conséquence. Gate : `check` VERT 754 tests 0 FAIL (baseline S-104 679 + 75
postérieurs, 0 régression) + mavenLocal VERT (POM 0.0.6 + platform 0.0.30).
Pas de split-brain local. Commit `bf6a337` poussé.

== ROLLOUT-3 plantuml-gradle (S-030)

Baby-step TDD strict RED → GREEN. Garde `PlantumlPluginPublicationTest` 3/3.
Pin ws + version dérivée + platform 0.0.21→0.0.30. **Split-brain local
éliminé** : toml `plantuml = "0.0.3"` vs build hardcode `0.0.4` (= Central)
— attrapé au RED, toml aligné. Gates : unit 2531/0 + functionalTest 85/1
(le FAIL `PlantumlFunctionalSuite$FilePermission` est préexistant, prouvé
par `git stash` avant=après) + mavenLocal VERT. Commit `52d6352` poussé.

== ROLLOUT-4 hyperframes-gradle (S-031)

Baby-step TDD strict RED → GREEN. Garde `HyperframesPluginPublicationTest`
3/3. Pin ws + version dérivée + platform 0.0.7→0.0.30. Pas de split-brain
local (toml = build = Central = ws = 0.0.3). Gate : `check` VERT 214 tests
0 FAIL + mavenLocal VERT. Commit `3a2d01d` poussé.

== ROLLOUT-5 runner-gradle (S-031) — cas C7

Le plus complexe (hardcode `id(...) version "0.0.9"` non couvert par la
platform) :

. toml workspace-bom : entrées `[plugins]` ajoutées (8 boroughs education.cccp,
  D7) + bump BOM 0.0.31 + garde 0.0.31 + gates BOM VERT (189 tests) +
  mavenLocal + publication Central 0.0.31 (repo1 HTTP 200 ×2, poll 10,
  entrée `bakery = {id = "education.cccp.bakery", version.ref = "bakery-plugin"}` vérifiée).
. runner settings : pin ws 0.0.31 (D4) — piège découvert : le runner n'avait
  PAS de `dependencyResolutionManagement.repositories` (seulement
  pluginManagement) → "no repositories are defined", corrigé (mavenLocal +
  mavenCentral ajoutés).
. runner build : `alias(ws.plugins.bakery)` (D7) remplace le hardcode C7.
. Gate : `tasks -PsiteName=magic-stick` VERT (deploySite + publishSite
  présents) + `buildEnvironment` prouve `education.cccp:bakery-plugin:0.0.9`
  résolu via le catalog ws.
Commit `ad9036b` poussé. Reste (cadré, non bloquant) : déclencher un run CI
deploy réel magic-stick/cheroliv.com pour confirmation bout en bout.

== US-CI-1.2 wrapper jar api-key-pool (S-031, quick win 0.5 pt)

`.gitignore` ligne 23 (`**/gradle/wrapper/gradle-wrapper.jar`) retirée + jar
committé (root + api-key-pool-plugin, 46 Ko Apache-2.0). La CI runAllTests
BOM n'échouera plus sur ce borough. Commit `cf20c3b` poussé.

== Récapitulatif EPIC MEM-CAT-ROLLOUT

|===
| Ordre | Borough | Statut | Commit
| 1 | document-gradle | ✅ TERMINÉ (S-244) | `e2d33c6`
| 2 | codex-gradle | ✅ TERMINÉ (S-211) | `bf6a337`
| 3 | plantuml-gradle | ✅ TERMINÉ (S-030) | `52d6352`
| 4 | hyperframes-gradle | ✅ TERMINÉ (S-031) | `3a2d01d`
| 5 | runner-gradle | ✅ TERMINÉ (S-031, cas C7) | `ad9036b`
| 6 | dashboard, readme, graphify... | ⏳ À PLANIFIER (à la demande) | —
|===

*Référentiel central* : `workspace-bom/BACKLOG.adoc` § EPIC MEM-CAT-ROLLOUT
(5/6 mis à jour). *Release Central* : BOM 0.0.31 + catalog 0.0.31 (bundle
AUTOMATIC, entrées `[plugins]` 8 boroughs, vérifiées repo1).

== Pattern d'adoption (réutilisable ROLLOUT-6+)

. Garde `<Plugin>PluginPublicationTest` 3 tests (pattern Document S-244) — RED
. settings : `create("ws") { from("education.cccp:workspace-catalog:0.0.31") }` (D4)
. build : `version = ws.versions.<plugin>.plugin.get()` (D3) + platform 0.0.31
. toml local aligné si split-brain (comme document/plantuml)
. Gate : `test`/`check` baseline exacte + `publishToMavenLocal` VERT
. Runner (ou tout consommateur `plugins {}`) : `alias(ws.plugins.<plugin>)` (D7)

== Pièges nouveaux (S-031/S-032)

11. *runner* : `dependencyResolutionManagement.repositories` manquant →
    catalog ws irrésolvable ("no repositories are defined") — à déclarer même
    si pluginManagement couvre pluginManagement.dependencies.
12. *alias dans block plugins {}* : nécessite une entrée `[plugins]` dans le
    toml du catalog publié (D7) — pas juste `[versions]`/`[libraries]`.
13. *publication catalog* : les entrées `[plugins]` sont bien transportées
    (toml 0.0.31 vérifié repo1, `bakery = {...version.ref = "bakery-plugin"}`).