= PROMPT_REPRISE — MEMPHIS (workspace-bom)

== Session en cours
026 — MEM-CAT-1 (fix split-brain intra-MEMPHIS)

== Mission session 026 — MEM-CAT-1

L'EPIC MEM-CATALOG est cadré (S-025, `.agents/CADRAGE_EPIC_MEM_VERSION_CATALOG.adoc`) :
le BOM devient unique source de vérité des versions plugins via un version
catalog publié (`education.cccp:workspace-catalog`), consommé par artefact
Maven (zéro référence physique au repo BOM — demande pilote).

S-026 = première US :

. Aligner le toml sur les valeurs réelles : `agent-contracts = "0.0.3"`
  (platform hardcode déjà 0.0.3, toml en retard), `workspace-bom` (version
  BOM réelle), absorber le WIP epub3 3.0.1.
. Faire consommer la platform le toml (`api(libs.*)` au lieu de hardcoder,
  lignes 19-125) + `version` dérivée du toml.
. UT `PlatformCatalogAlignmentTest` (toml↔platform cohérents).
. POM `generatePomFile` comparé avant/après — contraintes identiques
  (zéro changement de résolution).
. Gate : `./gradlew build -x test` VERT + POM diff vide.

Puis S-027 publication catalog (CAT-2), S-028 pilote bakery (CAT-3 —
COORDONNER avec la session bakery active, hardcode document-plugin 0.0.15
à remplacer par platform BOM) + CAT-4/5.

== État du code (S-025)

- BOM 0.0.27 dernière publiée Central ; working tree a un WIP epub3 3.0.1
  (toml + platform) non commité — à absorber dans CAT-1.
- 8 modules contrats N0 publiés (agent/codebase/llm-pool/opencode-session/
  i18n/pipeline/runtime/ocr contracts).
- `gradle/libs.versions.toml` référence déjà les 12 plugins education.cccp —
  ce sera la source du catalogue publié (CAT-2).

== Pièges à retenir

1. *Polling Central* : `<latest>` périmé ~27 min — vérifier aussi HTTP 200 POM.
2. *POM avant/après* : le fix CAT-1 ne doit RIEN changer aux contraintes résolues.
3. *Gradle refuse les versions dynamiques* dans `versionCatalogs { from(...) }` — pin explicite.
4. *Block `plugins {}`* : seul un catalog (alias) fournit une version de plugin — pas un platform.
5. *Entrées fantômes* : ne mettre au catalogue que des artefacts résolvables Central (capsule/jhipster pas encore).
6. *Sessions concurrentes* : bakery active (version sync 0.0.9 + pub Central) — coordonner avant CAT-3.

== Règles

- Voir `AGENT.adoc` — zéro initiative, permission explicite pour commit/push/publish.
- Baby-step DDD/TDD/BDD : test d'abord, build verify.
- Publication Maven Central : `./gradlew publishAggregationToCentralPortal` (aggrégatif,
  couvrira les 2 publications BOM + catalog).
- Zéro Python — outputs analysés Read/Grep/xmllint/rapports Gradle natifs.

== À vérifier en ouverture S-026

1. `git status` — WIP epub3 toujours présent ?
2. Central `<latest>` BOM (0.0.27 attendu, ou plus si publication entre-temps).
3. `./gradlew build -x test` VERT.
4. Lire `CADRAGE_EPIC_MEM_VERSION_CATALOG.adoc` (constats C1-C8, décisions D1-D12).