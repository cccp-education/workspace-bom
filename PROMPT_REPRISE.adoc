= PROMPT_REPRISE — MEMPHIS (workspace-bom)

== Session en cours
017 — PUB-3.06 pipeline-contracts + BOM 0.0.7 + DOC-8.1 vérification (terminée)

== Mission session 018
Mise à jour des consommateurs BOM 0.0.7 + vérification alignement contrats N0.

=== Priorité 1 — Migrer document-gradle vers BOM 0.0.7
. `document-plugin/gradle/libs.versions.toml` : `workspace-bom = "0.0.6"` → `"0.0.7"`.
. `document-plugin/build.gradle.kts` : `implementation(platform("education.cccp:workspace-bom:0.0.6"))` → `"0.0.7"`.
. `./gradlew compileKotlin` verify.
. Vérifier que pipeline-contracts:0.0.2 est bien résolu depuis la BOM.

=== Priorité 2 — Audit alignement BOM 0.0.7 cross-boroughs
. Lister tous les boroughs consommant `workspace-bom` (grep `workspace-bom` dans
  `foundry/public/**/build.gradle.kts` et `libs.versions.toml`).
. Pour chacun, vérifier la version référencée (0.0.6 vs 0.0.7).
. Planifier montée de version globale (0.0.7) — sans forcer, juste inventorier.

=== Priorité 3 — Bug cucumberTest Gradle 9.6.1
. `document-plugin` cucumberTest échoue avec `java.io.EOFException`
  (`getPreviousFailedTestClasses` — fichiers binaires vides).
. Investiguer : bug Gradle 9.6.1 connu ? Contournement propre ?
. Voir `conventions-gradle` CucumberConventionsPlugin — peut-être un
  `outputs.upToDateWhen { false }` ou `--rerun-tasks` systématique.

== Règles
- Voir `AGENT.adoc` — zéro initiative, permission explicite pour commit/push.
- Baby-step DDD/TDD/BDD : un type à la fois, test d'abord, build verify.
- Publication Maven Central : `./gradlew publishAggregationToCentralPortal`.