= PROMPT_REPRISE — MEMPHIS (workspace-bom)

== Session en cours
028 — TERMINÉE (audit transverse versions + fix split-brain conventions-plugin + release Central 0.0.30)

== État du code (S-028)

- Central : BOM 0.0.30 + workspace-catalog 0.0.30 (repo1 HTTP 200 ×2).
- toml : `workspace-bom = "0.0.30"`, `conventions-plugin = "0.0.4"`
  (split-brain 0.0.3 corrigé — le toml avait raté le bump S-011 conventions).
- Garde `PlatformCatalogAlignmentTest` épinglée sur conventions 0.0.4 + BOM 0.0.30.
- Bakery pilote : pin catalog 0.0.30 (settings) + platform 0.0.30 (build, 3 occ).
- Baseline bakery : 3 FAIL i18n flaky préexistants (avant = après, stash vérifié).

== Mission session 029 — maintenance / généralisation (à la demande)

Aucun EPIC ouvert. Piste prioritaire (demande pilote transverse) :

. *Généralisation catalog* borough par borough (D5, HORS EPIC) :
   document (platform 0.0.26), codex (0.0.26), plantuml (0.0.21),
   runner (hardcode `id(...) version "0.0.9"`), dashboard, hyperframes...
   1 pin settings + version dérivée par borough.
. *Dette bakery* : 3 FAIL i18n flaky (RealSiteI18nMigration,
   FixtureAlignmentAuditor, I18nDeltaWire) — EPIC bakery dédié, pas MEMPHIS.
. *US-CI-1.2/1.4* (BACKLOG) : wrapper jar api-key-pool + qwen3:0.6b CI.
. *DOC-8* (P2) : document-gradle consommer les contrats N0.

== Pièges à retenir (cumul S-026 + S-027 + S-028)

1. *Gradle 9 API* : publication catalog = plugin `version-catalog` +
   `catalog { versionCatalog { from(...) } }` + `from(components["versionCatalog"])`.
   `MavenPublication.versionCatalog {}` N'EXISTE PLUS.
2. *Versions fantômes* : epub3 `3.0.1` n'existe pas Central (→ 2.2.0) ;
   capsule-plugin 404 Central (entrée retirée du toml — D8).
3. *Conflit classpath Gradle 9.6.1* : strictly 2.3.21 embarqué vs platform
   2.4.10 → forces buildscript sur racines consommateurs (bakery, runner).
4. *Extension Gradle* : `Provider<T>` brut NON décorable — utiliser `Property<T>`.
5. *runAllTests* : liste restreinte à 5 boroughs verts (dettes documentées
   build.gradle.kts BOM).
6. *nmcp collision* : 2 uploads même coordonnée → 2e échoue tant que le 1er
   est en course ; API `publisher/status` peut renvoyer HTTP 500 — vérifier
   repo1 avant de re-tenter.
7. *Init scripts* : accesseurs `ws.*` non visibles dans les init scripts —
   preuve propagation via settings consommateur réel ou test FT.
8. *2 suites Cucumber même source set* : `cucumber.filter.tags` system property
   prime sur la config du runner (2 glues coexistantes sans source set séparé).
9. *S-028* : `:agent-contracts:test` n'existe PAS depuis la racine BOM —
   agent-contracts est un build standalone (lancer depuis `agent-contracts/`).
   Idem bakery : `test` n'existe qu'à la racine `bakery-plugin/`.
10. *S-028* : les tests gardes `PlatformCatalogAlignmentTest` épinglent les
   versions (BOM + conventions) — après CHAQUE bump toml, bump la garde.

== Règles

- Voir `AGENT.adoc` — zéro initiative, permission explicite pour commit/push/publish.
- Baby-step DDD/TDD/BDD : test d'abord, build verify.
- Publication Central : `./gradlew publishAggregationToCentralPortal` (aggrégatif
  — couvre BOM + catalog en 1 bundle). Protocole : `.agents/RELEASE_PROTOCOL.adoc`.
- Zéro Python — outputs analysés Read/Grep/xmllint/rapports Gradle natifs.
- Le pilote autorise commit/push en fin de session + bump/publication
  mavenLocal/Central dès que nécessaire (S-028).

== À vérifier en ouverture S-029

1. `git status` — workspace-bom committé/poussé ? (S-028 clôturé)
2. Central `<latest>` BOM + catalog (0.0.30 attendu).
3. `./gradlew test versionCatalogCucumberTest` depuis `agent-contracts/` VERT.
4. Demande pilote si généralisation catalog vers un nouveau borough.
5. Backlog : EPIC MEM-CATALOG marqué ✅ TERMINÉ (S-027) — ne pas rouvrir.