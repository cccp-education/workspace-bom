= PROMPT_REPRISE — MEMPHIS (workspace-bom)

== Session en cours
016 — MEM-2 Pipeline Contracts (restauration) (terminée)

== Mission session 017
Publication Maven Central de pipeline-contracts:0.0.2 + câblage DOC-8.1
dans document-gradle (releaseNotesGenerate).

=== Priorité 1 — Publication Maven Central (PUB-3.06)
. `cd foundry/public/workspace-bom/pipeline-contracts`
. `./gradlew publishAggregationToCentralPortal --no-daemon`
. Vérifier https://central.sonatype.com/artifact/education.cccp/pipeline-contracts
. MàJ BACKLOG.adoc PUB-3.06 → ✅ PUBLIÉ.

=== Priorité 2 — Câblage DOC-8.1 (document-gradle)
. Ajouter `pipeline-contracts = "0.0.2"` dans
  `document-plugin/gradle/libs.versions.toml` [versions] + [libraries].
. Ajouter `implementation(libs.pipeline.contracts)` dans
  `document-plugin/build.gradle.kts` (après i18n + opencode-session).
. Créer implémentation concrète `ReleaseNotesGenerate` :
  - `JGitLogParser` (JGit 7.5.0 — `org.eclipse.jgit` déjà dans le BOM).
  - `AsciidoctorReleaseNotesRenderer` (AsciiDoc `= Release X.Y.Z` + sections
    par catégorie de [ReleaseNotesConfig.categories]).
  - `DocumentReleaseNotesGenerator` (compose parser + renderer, écrit
    `build/release-notes/RELEASE_NOTES.adoc`).
. Task Gradle `releaseNotesGenerate` dans `DocumentPlugin.kt`.
. MàJ `DocumentI18nBridge.kt` : supprimer le TODO "pipeline-contracts pas
  encore publie".
. Tests : unit (JGitLogParser avec repo temp JGit) + functional (GradleRunner)
  + cucumber (scénario release notes complet).

=== Priorité 3 — BOM 0.0.7
. Bumper version BOM racine `0.0.6 → 0.0.7` + publier (ajout constraint
  pipeline-contracts:0.0.2).

== Règles
- Voir `AGENT.adoc` — zéro initiative, permission explicite pour commit/push.
- Baby-step DDD/TDD/BDD : un type à la fois, test d'abord, build verify.
- Publication Maven Central : `./gradlew publishAggregationToCentralPortal`.