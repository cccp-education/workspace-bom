= PROMPT_REPRISE — MEMPHIS (workspace-bom)

== Session en cours
027 — TERMINÉE (MEM-CAT-4 + CAT-5 + publication Central 0.0.29 — EPIC MEM-CATALOG 5/5)

== Mission session 028 — maintenance / adoption

EPIC MEM-CATALOG TERMINÉ (S-027). Le BOM est désormais la source de vérité des
versions plugins cross-borough, transportée par 2 artefacts Maven ensemble :

. `education.cccp:workspace-bom:0.0.29` — platform (deps sans version)
. `education.cccp:workspace-catalog:0.0.29` — catalog (`ws.versions.*`, `ws.plugins.*`)

== État du code (S-027)

- Platform 100% `api(libs.*)`, version BOM dérivée du toml (gardes D10/D11a).
- Catalog publié (plugin `version-catalog`, API Gradle 9) — même release BOM.
- `planner-plugin = "0.0.2"` dans le toml (bump CAT-4, propagé Central — preuve).
- Runner Cucumber dédié `versionCatalogCucumberTest` (3 scénarios `@version-catalog`).
- `.agents/RELEASE_PROTOCOL.adoc` — protocole release officiel (6 étapes).
- Bakery pilote : pin ws 0.0.29, version dérivée, document-plugin sans version.
- Central : BOM 0.0.29 + catalog 0.0.29 HTTP 200 (bundle `e6f571c2` AUTOMATIC).

== Prochaines missions possibles (aucune urgente)

. *Généralisation catalog* (HORS EPIC, D5 — à la demande par borough) :
  runner-gradle (hardcode `id(...) version "0.0.9"` C7), document, dashboard,
  codebase, codex... 1 pin settings + version dérivée par borough.
. *Dette bakery* : cucumber flaky (i18n-delta/dns/jbake-native, 6-7 FAIL
  variables) — EPIC bakery dédié, pas MEMPHIS.
. *US-CI-1.2/1.4* (BACKLOG) : wrapper jar api-key-pool + qwen3:0.6b CI — restés
  🔄 À FAIRE depuis S-014.
. *DOC-8* (P2) : document-gradle consommer les contrats N0.

== Pièges à retenir (cumul S-026 + S-027)

1. *Gradle 9 API* : publication catalog = plugin `version-catalog` +
   `catalog { versionCatalog { from(...) } }` + `from(components["versionCatalog"])`.
   `MavenPublication.versionCatalog {}` N'EXISTE PLUS.
2. *Versions fantômes* : epub3 `3.0.1` n'existe pas Central (→ 2.2.0) ;
   capsule-plugin 404 Central (entrée retirée du toml — D8).
3. *Conflit classpath Gradle 9.6.1* : strictly 2.3.21 embarqué vs platform
   2.4.10 → forces buildscript sur racines consommateurs (bakery, runner).
4. *Extension Gradle* : `Provider<T>` brut NON décorable — utiliser `Property<T>`.
5. *runAllTests* : liste restreinte à 5 boroughs verts (dettes documentées
   build.gradle.kts BOM).
6. *nmcp collision* : 2 uploads de la même coordonnée → le 2e échoue tant que
   le 1er est en course ; l'API `publisher/status` peut renvoyer HTTP 500 —
   vérifier repo1 avant de re-tenter.
7. *Init scripts* : accesseurs `ws.*` non visibles dans les init scripts —
   preuve propagation via settings consommateur réel ou test FT.
8. *2 suites Cucumber même source set* : `cucumber.filter.tags` system property
   prime sur la config du runner (2 glues coexistantes sans source set séparé).

== Règles

- Voir `AGENT.adoc` — zéro initiative, permission explicite pour commit/push/publish.
- Baby-step DDD/TDD/BDD : test d'abord, build verify.
- Publication Central : `./gradlew publishAggregationToCentralPortal` (aggrégatif
  — couvre BOM + catalog en 1 bundle). Protocole : `.agents/RELEASE_PROTOCOL.adoc`.
- Zéro Python — outputs analysés Read/Grep/xmllint/rapports Gradle natifs.

== À vérifier en ouverture S-028

1. `git status` — workspace-bom committé/poussé ? (S-027 clôturé)
2. Central `<latest>` BOM + catalog (0.0.29 attendu).
3. `./gradlew :agent-contracts:test versionCatalogCucumberTest` VERT.
4. Demande pilote si généralisation catalog vers un nouveau borough.
5. Backlog : EPIC MEM-CATALOG marqué ✅ TERMINÉ (S-027) — ne pas rouvrir.