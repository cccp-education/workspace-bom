= PROMPT_REPRISE — MEMPHIS (workspace-bom)

== Session en cours
027 — MEM-CAT-4 (preuve de propagation) — accord pilote requis si publication Central

== Mission session 027 — MEM-CAT-4 + CAT-5

EPIC MEM-CATALOG 3/5 US terminé (S-026 : CAT-1/2/3, ~13/21 pts) :

. *MEM-CAT-4* — bump 1 version plugin dans le toml MEMPHIS → publication
  catalogue → le consommateur pilote (bakery) voit la nouvelle version SANS
  éditer son build (seul le pin settings peut suivre). Test FT chaîne complète
  + doc protocole release (bump BOM + catalog ensemble, même bundle nmcp).
. *MEM-CAT-5* — Cucumber BDD `@version-catalog` (catalog résolvable / accessor
  typé / hygiène pas-de-hardcode) + README migration autres boroughs.
. Publication Central : BOM 0.0.29 + workspace-catalog 0.0.29 (un seul
  `publishAggregationToCentralPortal` — nmcp 1.5.0 agrège les 2 publications).
  *Accord pilote explicite requis.*

== État du code (S-026)

- Platform 100% `api(libs.*)` — toml = source unique (split-brain C1/C2 tués).
- `workspace-catalog:0.0.29` publié mavenLocal (packaging toml, POM complet).
- Bakery pilote : pin `ws` 0.0.29, `version = ws.versions.bakery.plugin.get()`,
  document-plugin sans version (constraint platform), PublicationTest migré.
- 9 tests garde VERT (PlatformCatalogAlignmentTest 5 + WorkspaceCatalogStructureTest 4).
- BOM 0.0.29 mavenLocal ; Central latest = 0.0.27 (polling ~27 min).

== Pièges à retenir

1. *Gradle 9 API* : publication catalog = plugin `version-catalog` +
   `catalog { versionCatalog { from(...) } }` + `from(components["versionCatalog"])`.
   `MavenPublication.versionCatalog {}` N'EXISTE PLUS.
2. *Versions fantômes* : epub3 `3.0.1` n'existe pas Central (→ 2.2.0) ;
   capsule-plugin 404 Central (entrée retirée du toml — D8).
3. *Conflit classpath Gradle 9.6.1* : strictly 2.3.21 embarqué vs platform
   2.4.10 → forces buildscript sur racines consommateurs (bakery, runner).
4. *Extension Gradle* : `Provider<T>` brut NON décorable — utiliser `Property<T>`.
5. *runAllTests* : liste restreinte à 5 boroughs verts (dettes documentées
   build.gradle.kts BOM).

== Règles

- Voir `AGENT.adoc` — zéro initiative, permission explicite pour commit/push/publish.
- Baby-step DDD/TDD/BDD : test d'abord, build verify.
- Publication Central : `./gradlew publishAggregationToCentralPortal` (aggrégatif
  — couvre BOM + catalog en 1 bundle).
- Zéro Python — outputs analysés Read/Grep/xmllint/rapports Gradle natifs.

== À vérifier en ouverture S-027

1. `git status` — 4 repos touchés S-026 (bom/bakery/codebase/dashboard) committés ?
2. Central `<latest>` BOM (0.0.27 attendu — 0.0.29 non encore publié).
3. `./gradlew build` BOM VERT.
4. Lire `CADRAGE_EPIC_MEM_VERSION_CATALOG.adoc` (D4 propagation, US CAT-4/5).
5. Coordination : bakery session — le pilote bakery doit repartir de ce pin ws.