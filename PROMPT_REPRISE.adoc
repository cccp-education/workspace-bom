= PROMPT_REPRISE — MEMPHIS (workspace-bom)

== Session en cours
014 — CI runAllTests workflow + 1er run GitHub Actions

== Mission session 015
Fixer les 3 échecs CI du run #28412781846 + ajouter Ollama qwen3:0.6b.

=== Priorité 1 — Fixer les 3 échecs CI
. **api-key-pool-gradle** : `gradle-wrapper.jar` gitignoré → clone CI sans jar.
  Fix : retirer ligne `.gitignore` `**/gradle/wrapper/gradle-wrapper.jar` et
  committer le jar, OU ajouter step CI `gradle wrapper --gradle-version 9.5.1`.
. **readme-gradle** : `Task 'check' not found` — racine est consommateur
  (`alias(libs.plugins.readme)`), pas de tâche `check`. Vrais tests dans
  `readme-plugin/` (propre `gradlew` + `build.gradle.kts`).
. **slider-gradle** : idem — vrais tests dans `slider-plugin/`.
. Fix proposé : `testableProjects` → `List<Pair<String, String?>>` avec
  sous-répertoire cible. `readme-gradle`→`"readme-plugin"`,
  `slider-gradle`→`"slider-plugin"`. `Exec.workingDir` → `projectDir.resolve(subdir)`.

=== Priorité 2 — Ollama qwen3:0.6b en CI
. Step CI : `docker run -d -p 11466:11467 ollama/ollama` + `ollama pull qwen3:0.6b`.
. Env vars : `OLLAMA_TEST_PORT=11466`, `OLLAMA_TEST_MODEL=qwen3:0.6b`.
. Tests `assumeTrue(isOllamaReady(port))` se débloquent. Pas de secret requis.

=== Priorité 3 — Secrets
`GITHUB_TOKEN` natif suffit. Aucun secret additionnel pour runAllTests.

== État CI
- Run #28412781846 : ÉCHEC (37m46s) — 3 boroughs échouent, 10 OK.
- Workflow : `.github/workflows/test.yml` sur main.
- Logs : https://github.com/cccp-education/workspace-bom/actions/runs/28412781846

== Règles
- Voir `AGENT.adoc` — zéro initiative, permission explicite pour commit/push.
- Debug depuis GitHub remote, arrêter de solliciter les ressources locales.