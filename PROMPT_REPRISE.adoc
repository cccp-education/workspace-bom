= PROMPT_REPRISE — MEMPHIS (workspace-bom)

== Session en cours
104 (cross-borough codex) — 9e artefact N0 `ocr-contracts:0.0.1` + BOM 0.0.26 publiés (terminée)

== Session 104 terminée (exécutée depuis Brooklyn codex)

9e artefact N0 `ocr-contracts:0.0.1` publié Maven Central (bundle `0d81da46`)
— package `contracts.ocr`, 3 types purs port OCR cross-borough (`OcrEngine`
fun interface, `OcrRequest`, `OcrResult` + factory `of`), 14 unit VERT,
module standalone pattern `runtime-contracts`.
BOM bump 0.0.25 → 0.0.26 publié Central (bundle `43e7da4c`) — constraint
`ocr-contracts:0.0.1` + constraint `codex-plugin` anticipée 0.0.6.
Débloque RAG-4 final (codebase retrait `libs.codex.plugin` — EPIC
CDX-RAG-SOCLE TERMINÉ) et clôture l'EPIC CDX-OCR-CONTRACTS.

== Mission session MEMPHIS suivante

Rattrapage INDEX MEMPHIS (en retard — s'arrête à S-018/0.0.9) :
synchroniser INDEX/SESSIONS_HISTORY avec les versions réelles publiées
(BOM 0.0.26, runtime-contracts 0.0.1, pipeline-contracts 0.0.2,
agent-contracts 0.0.3, codebase-contracts 0.0.2, ocr-contracts 0.0.1,
codex 0.0.6).

=== Priorité 1 — Vérifier propagation Maven Central
. `ocr-contracts:0.0.1` disponible sur repo1.maven.org (propagation 15-30 min)
. `workspace-bom:0.0.26` disponible sur repo1.maven.org
. `codex-plugin:0.0.6` disponible sur repo1.maven.org

=== Priorité 2 — Consommateurs BOM 0.0.26
. Lister les boroughs référençant workspace-bom < 0.0.26.
. codebase-plugin 0.0.9 à publier (POM slim sans codex transitif — accord pilote).

== Règles
- Voir `AGENT.adoc` — zéro initiative, permission explicite pour commit/push.
- Baby-step DDD/TDD/BDD : un type à la fois, test d'abord, build verify.
- Publication Maven Central : `./gradlew publishAggregationToCentralPortal`.
