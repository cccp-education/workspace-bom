= PROMPT_REPRISE — MEMPHIS (workspace-bom)

== Session en cours
018 — BOM 0.0.9 + slider 0.0.10 (terminée)

== Session 018 terminée

BOM 0.0.9 publiée (bundle `79395cf5`). slider constraint 0.0.1 → 0.0.10 +
correction module name (`slider-plugin` → `slider`). BOM 0.0.8 → 0.0.9.

== Mission session 019

Audit alignement cross-boroughs + mise à jour des consommateurs BOM.

=== Priorité 1 — Audit alignement BOM 0.0.9 cross-boroughs
. Lister tous les boroughs consommant `workspace-bom` (grep `workspace-bom`
  dans `foundry/public/**/build.gradle.kts` et `libs.versions.toml`).
. Pour chacun, vérifier la version référencée (0.0.6/0.0.7 vs 0.0.9).
. Planifier montée de version globale (0.0.9) — sans forcer, juste inventorier.
. Vérifier que les autres plugins (bakery 0.0.3, codex 0.0.2, planner 0.0.1,
  plantuml 0.0.1, readme 0.0.1, hyperframes 0.0.3, graphify 0.0.2,
  api-key-pool 0.0.1, codebase 0.0.3, capsule 0.0.1) sont à jour vs Central.

=== Priorité 2 — Bug cucumberTest Gradle 9.6.1
. `document-plugin` cucumberTest échoue avec `java.io.EOFException`
  (`getPreviousFailedTestClasses` — fichiers binaires vides).
. Investiguer : bug Gradle 9.6.1 connu ? Contournement propre ?
. Voir `conventions-gradle` CucumberConventionsPlugin.

=== Priorité 3 — Migrer document-gradle vers BOM 0.0.9
. `document-plugin/gradle/libs.versions.toml` : `workspace-bom` version → `"0.0.9"`.
. `document-plugin/build.gradle.kts` : `platform("education.cccp:workspace-bom:0.0.9")`.
. `./gradlew compileKotlin` verify.

== Règles
- Voir `AGENT.adoc` — zéro initiative, permission explicite pour commit/push.
- Baby-step DDD/TDD/BDD : un type à la fois, test d'abord, build verify.
- Publication Maven Central : `./gradlew publishAggregationToCentralPortal`.