= PROMPT_REPRISE — MEMPHIS (workspace-bom)

== Session en cours
032 — TERMINÉE (EPIC MEM-CAT-ROLLOUT 5/6 : ROLLOUT-2 codex, ROLLOUT-3 plantuml, ROLLOUT-4 hyperframes, ROLLOUT-5 runner cas C7 + US-CI-1.2 + release Central 0.0.31 avec entrées [plugins])

== État du code (S-032)

- Central : BOM 0.0.31 + workspace-catalog 0.0.31 (repo1 HTTP 200 ×2, entrées
  `[plugins]` 8 boroughs education.cccp transportées — `bakery = {id = ...,
  version.ref = "bakery-plugin"}` vérifié).
- 4 boroughs adoptés : codex (754/0), plantuml (2531/0, FT 85/1 baseline),
  hyperframes (214/0), runner (alias ws.plugins.bakery, tasks VERT). document
  (3272/0) adopté en S-244. US-CI-1.2 wrapper jar api-key-pool committé.
- Garde `PlatformCatalogAlignmentTest` épinglée BOM 0.0.31 + conventions 0.0.4.
- ROLLOUT-6+ (dashboard, readme, graphify...) ⏳ À PLANIFIER à la demande.

== Mission session 033 — à la demande

. *ROLLOUT-6+* boroughs restants (dashboard, readme, graphify, bureau...) :
   pattern d'adoption réutilisable documenté rapport S-032 (garde RED → pin ws
   → version dérivée → platform 0.0.31 → gate → mavenLocal).
. *Confirmation CI runner* : déclencher un run deploy réel magic-stick /
   cheroliv.com (alias ws.plugins.bakery en conditions réelles gh-pages).
. *US-CI-1.4* : Ollama qwen3:0.6b en CI (dépend workflow test.yml, 1 pt).
. *Dette bakery* : EPIC BKY-TEST-FLAKY (3 FAIL i18n, BACKLOG+TODO bakery S-028).
. *DOC-8 reste* (P3) : opencode-session-contracts (deps présentes, 0 usage).

== Pièges à retenir (cumul S-026 + S-027 + S-028 + S-032)

1. *Gradle 9 API* : publication catalog = plugin `version-catalog` +
   `catalog { versionCatalog { from(...) } }` + `from(components["versionCatalog"])`.
   `MavenPublication.versionCatalog {}` N'EXISTE PLUS.
2. *Versions fantômes* : epub3 `3.0.1` n'existe pas Central (→ 2.2.0) ;
   capsule-plugin 404 Central (entrée retirée du toml — D8).
3. *Conflit classpath Gradle 9.6.1* : strictly 2.3.21 embarqué vs platform
   2.4.10 → forces buildscript sur racines consommateurs (bakery, runner).
4. *Extension Gradle* : `Provider<T>` brut NON décorable — utiliser `Property<T>`.
5. *runAllTests* : liste restreinte à 5 boroughs verts (dettes documentées
   build.gradle.kts BOM).
6. *nmcp collision* : 2 uploads même coordonnée → 2e échoue tant que le 1er
   est en course ; API `publisher/status` peut renvoyer HTTP 500 — vérifier
   repo1 avant de re-tenter.
7. *Init scripts* : accesseurs `ws.*` non visibles dans les init scripts —
   preuve propagation via settings consommateur réel ou test FT.
8. *2 suites Cucumber même source set* : `cucumber.filter.tags` system property
   prime sur la config du runner (2 glues coexistantes sans source set séparé).
9. *`:agent-contracts:test` n'existe pas depuis la racine BOM* — build
   standalone (lancer depuis `agent-contracts/`). Idem bakery : `test`
   n'existe qu'à la racine `bakery-plugin/`.
10. *Gardes épinglent les versions* (BOM + conventions + contrats) — après
   CHAQUE bump toml, bump la garde dans le même commit.
11. *runner* : `dependencyResolutionManagement.repositories` manquant →
    catalog ws irrésolvable ("no repositories are defined") — même si
    pluginManagement couvre les plugins.
12. *alias dans block plugins {}* : nécessite une entrée `[plugins]` dans le
    toml du catalog publié (D7) — le block `plugins {}` n'accède pas aux
    accesseurs version, seul alias marche.
13. *Transport plugins* : les entrées `[plugins]` du toml sont bien publiées
    et consommables via `ws.plugins.*` (vérifié repo1 0.0.31).

== Règles

- Voir `AGENT.adoc` — zéro initiative, permission explicite pour commit/push/publish.
- Baby-step DDD/TDD/BDD : test d'abord, build verify.
- Publication Central : `./gradlew publishAggregationToCentralPortal` (aggrégatif
  — couvre BOM + catalog en 1 bundle). Protocole : `.agents/RELEASE_PROTOCOL.adoc`.
- Zéro Python — outputs analysés Read/Grep/xmllint/rapports Gradle natifs.
- Le pilote autorise commit/push en fin de session + bump/publication
  mavenLocal/Central dès que nécessaire (S-028).

== À vérifier en ouverture S-033

1. `git status` — workspace-bom committé/poussé ? (S-032 clôturé)
2. Central `<latest>` BOM + catalog (0.0.31 attendu).
3. `./gradlew test versionCatalogCucumberTest` depuis `agent-contracts/` VERT.
4. Demande pilote : ROLLOUT-6+ (quels boroughs) OU confirmation CI runner.
5. Backlog : EPIC MEM-CATALOG ✅ ne pas rouvrir ; MEM-CAT-ROLLOUT 5/6 (6e à la demande).