= PROMPT_REPRISE — MEMPHIS (workspace-bom)

== Session en cours
218 — TERMINÉE (BOM/catalog 0.0.47 — codex-plugin 0.0.10). Release pilotée
Brooklyn (codex S-222), fix CDX-CONTEXT-HARDENING.

== État du code (S-218)

- Central : BOM + catalog 0.0.47 (dernier état), codex-plugin 0.0.10.
  Bundle S-218 : `48b061ce-20c5-4fd0-92d3-8a7508b21c88` (BOM/cat 0.0.47).
- Garde `PlatformCatalogAlignmentTest` réalignée : workspace-bom 0.0.47.
- 0.0.47 = codex 0.0.10 (input `enrichedJsonFile` tolérant `@InputFiles`,
  S-221) ; 0.0.46 = codex 0.0.9 corrective.

== Dette constatée — gouvernance MEMPHIS en retard

`INDEX.adoc` / `SESSIONS_HISTORY.adoc` s'arrêtent à S-218 (2026-09-17). Les
releases antérieures 0.0.35 → 0.0.46 ne sont pas toutes tracées. Une session
de resync de gouvernance MEMPHIS est recommandée (elle reste en tête de la
prochaine session).

== Mission session 219 — à la demande

. *Resync gouvernance MEMPHIS* (recommandé) : delta BOM 0.0.35→0.0.47,
   réalignement SESSIONS_HISTORY/INDEX.
. *Confirmation CI runner* : déclencher un run deploy réel magic-stick /
   cheroliv.com (alias ws.plugins.bakery en conditions réelles gh-pages).
. *US-CI-1.4* : Ollama qwen3:0.6b en CI (1 pt).
. *Audit complet adoption catalog* : vérifier qu'aucun borough education.cccp
   ne hardcode sa version propre hors ws (D5/D12).

== Pièges à retenir (cumul + S-217)

1. *Gradle 9 API* : publication catalog = plugin `version-catalog` +
   `from(components["versionCatalog"])`. `MavenPublication.versionCatalog {}` N'EXISTE PLUS.
2. *Versions fantômes* : epub3 `3.0.1` n'existe pas Central (→ 2.2.0) ;
   capsule-plugin 404 Central (entrée retirée du toml — D8).
3. *Conflit classpath Gradle 9.6.1* : strictly 2.3.21 embarqué vs platform
   2.4.10 → forces buildscript sur racines consommateurs (bakery, runner).
4. *Extension Gradle* : `Provider<T>` brut NON décorable — utiliser `Property<T>`.
5. *runAllTests* : liste restreinte (dettes documentées build.gradle.kts BOM).
6. *nmcp collision* : NE JAMAIS re-invoquer publishAggregationToCentralPortal
   sans vérifier repo1 d'abord (2 bundles même coordonnée — mordu S-215).
7. *Init scripts* : accesseurs `ws.*` non visibles dans les init scripts.
8. *2 suites Cucumber même source set* : `cucumber.filter.tags` system property prime.
9. *`:agent-contracts:test` n'existe pas depuis la racine BOM* — build standalone.
10. *Gardes épinglent les versions* (BOM + conventions + contrats) — après
    CHAQUE bump toml, bump la garde dans le même commit. *Mordu S-216* :
    baseline rouge à HEAD depuis S-213/S-217 (2 gardes jamais réalignées).
11. *runner* : `dependencyResolutionManagement.repositories` manquant →
    catalog ws irrésolvable.
12. *alias dans block plugins {}* : nécessite une entrée `[plugins]` (D7).
13. *settings nmcp* : pattern `java.util.Properties` + `~/.gradle/gradle.properties`.
14. *Polling repo1* : ~7-13 min après « deployment is publishing » (AUTOMATIC).
15. *Version dérivée* : la version propre d'un borough vient du catalogue
    (`ws.versions.<plugin>.get()`) — publier exige le bump MEMPHIS d'abord.
16. *Corrective si P0* : Central est immuable — un bug publié exige une
    nouvelle release (bump MEMPHIS + codex), jamais un re-upload même bundle.

== Règles

- Voir `AGENT.adoc` — permission explicite pour commit/push/publish.
- Baby-step DDD/TDD/BDD : test d'abord, build verify.
- Publication Central : `./gradlew publishAggregationToCentralPortal` (aggrégatif
  — couvre BOM + catalog en 1 bundle). Protocole : `.agents/RELEASE_PROTOCOL.adoc`.
- Zéro Python — outputs analysés Read/Grep/xmllint/rapports Gradle natifs.
- Le pilote autorise commit/push en fin de session + bump/publication dès que nécessaire.

== À vérifier en ouverture S-219

1. `git status` — workspace-bom + codex-gradle committés/poussés ?
2. Central `<latest>` BOM + catalog 0.0.47 + codex 0.0.10.
3. `./gradlew test --tests 'contracts.catalog.*'` depuis `agent-contracts/` VERT.
4. Demande pilote : resync gouvernance MEMPHIS OU CI runner OU US-CI-1.4.

