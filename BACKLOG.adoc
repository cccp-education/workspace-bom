= Backlog — MEMPHIS (workspace-bom)
:description: Backlog de publication Maven Central — EPIC PUB + MEM-2
:date: 2026-06-14
:toc: left
:sectnums:

_Dernière mise à jour : 2026-09-02 (S-028 — audit transverse versions : fix split-brain conventions-plugin 0.0.3→0.0.4 + release Central BOM+catalog 0.0.30 + bakery pilote aligné. EPIC MEM-CATALOG TERMINÉ S-026/S-027 — ne pas rouvrir.)_

_Source : configuration/EPIC_PUB.adoc (Session 096)_

'''
== EPIC PUB — Publication Maven Central

BOM + 5 libraries de contrats partagés (N0). Premier artefact publié (VAGUE 1). Toutes les autres vagues dépendent de ces artefacts.

=== PUB-3.01 — workspace-bom
**Priorite** : P0
**Estimation** : 1 pt
**Dependances** : PUB-0 et PUB-1 complets (DNS TXT + GPG)
**Statut** : ✅ PUBLIÉ + VÉRIFIÉ (dernière version 0.0.30 — S-028, 2026-09-02, audit transverse + fix conventions-plugin)

. Artefact : `education.cccp:workspace-bom:0.0.30` (dernière)
. Versions publiées : 0.0.1 → 0.0.30 (historique complet INDEX.adoc § Publications Maven Central)
. Verifiée sur https://central.sonatype.com/artifact/education.cccp/workspace-bom

=== PUB-3.02 — agent-contracts
**Priorite** : P0
**Estimation** : 1 pt
**Dependances** : PUB-0 et PUB-1 complets
**Statut** : ✅ PUBLIÉ + VÉRIFIÉ

. Artefact : `education.cccp:agent-contracts:0.0.1`

=== PUB-3.03 — codebase-contracts
**Priorite** : P0
**Estimation** : 1 pt
**Dependances** : PUB-0 et PUB-1 complets
**Statut** : ✅ PUBLIÉ + VÉRIFIÉ

. Artefact : `education.cccp:codebase-contracts:0.0.1`

=== PUB-3.04 — vibecoding-contracts
**Priorite** : P0
**Estimation** : 1 pt
**Dependances** : PUB-0 et PUB-1 complets
**Statut** : ✅ PUBLIÉ + VÉRIFIÉ

. Artefact : `education.cccp:vibecoding-contracts:0.0.1`

=== PUB-3.05 — llm-pool-contracts
**Priorite** : P0
**Estimation** : 1 pt
**Dependances** : PUB-0 et PUB-1 complets
**Statut** : ✅ PUBLIÉ + VÉRIFIÉ

. Artefact : `education.cccp:llm-pool-contracts:0.0.1`

=== PUB-3.06 — pipeline-contracts
**Priorite** : P1
**Estimation** : 1 pt
**Dependances** : MEM-2 terminé (33/33 PASS)
**Statut** : ✅ PUBLIÉ + VÉRIFIÉ (session 017 — bundle `f235a1da-7ff3-42cb-8b5b-5b91fb28b4af`)

. Artefact : `education.cccp:pipeline-contracts:0.0.2`
. Vérifié sur https://repo1.maven.org/maven2/education/cccp/pipeline-contracts/0.0.2/
. *Note* : pipeline-contracts avait été supprimé du BOM en session 084
  (refactor shared kernel → 3 artefacts, rationale "zéro usage"). Restauré
  session 016 car document-gradle DOC-8.1 en avait besoin.Publié session 017.

=== PUB-2.2 — Mise à jour commentaire relocation dans build.gradle.kts
**Priorite** : P1
**Estimation** : 1 pt
**Dependances** : Aucune
**Statut** : ✅ TERMINÉ

'''
== 🟡 EPIC MEM-CAT-ROLLOUT — Généralisation version catalog borough par borough (D5, adoption paresseuse)

_Origine : S-028 (audit transverse versions) + demande pilote transverse._
_Le pilote bakery (S-208/S-028) prouve le pattern : 1 pin settings + version
dérivée + platform sans version. Objectif : éliminer définitivement les bumps
éparpillés type split-brain conventions S-028._

*Référentiel des adoptions* (1 borough = 1 session courte, gate tests verts,
mise à jour BACKLOG + INDEX borough à chaque fois) :

[cols="1,3,2,2,2"]
|===
| Ordre | Borough | État actuel | Cible | Statut
| 1 | document-gradle | platform BOM 0.0.26 | pin ws + version dérivée + platform 0.0.30 | ✅ TERMINÉ (S-244 — garde 3/3, test 3272/0, FT 126/126, mavenLocal VERT, split-brain local 0.0.3/0.0.15 éliminé)
| 2 | codex-gradle | platform BOM 0.0.26 | idem | ✅ TERMINÉ (S-211 — garde 3/3, check 754/0, mavenLocal VERT POM 0.0.6/platform 0.0.30, version déjà dérivée toml local → ws sans split-brain)
| 3 | plantuml-gradle | platform BOM 0.0.21 | idem | ✅ TERMINÉ (S-030 — garde 3/3, unit 2531/0, FT 85/1 baseline stash-confirmée, split-brain local 0.0.3/0.0.4 éliminé)
| 4 | hyperframes-gradle | platform BOM 0.0.7 | idem | ✅ TERMINÉ (S-031 — garde 3/3, check 214/0, mavenLocal VERT, pas de split-brain local)
| 5 | runner-gradle | hardcode `id(...) version "0.0.9"` (C7) | pin ws + alias(ws.plugins.bakery) | 🟡 À FAIRE (prochaine)
| 6 | dashboard, readme, graphify... | divers | idem, à la demande | ⏳ À PLANIFIER
|===

*Pattern à reproduire* (voir bakery `bakery-plugin/settings.gradle.kts` +
`build.gradle.kts` S-026) : settings `create("ws") { from("education.cccp:
workspace-catalog:0.0.30") }` + `version = ws.versions.<plugin>.plugin.get()` +
platform BOM sans version (ou pin 0.0.30). *Gate* : `check`/`test` vert (baseline
exacte) + `publishToMavenLocal` si release.

'''
== EPIC DOC-I18N — README multilingues workspace-bom

*Session 010 (2026-06-24)* — Documentation multilingue alignée sur `i18n-contracts` (MEM-3).

=== DOC-I18N-1 — README Consumer (10 langues)
**Statut** : ✅ TERMINÉ (session 010)
. 10 fichiers `README.{en,zh,hi,es,fr,ar,bn,pt,ru,ur}.md`
. Master source EN + 9 traductions marquées `rev 0.0.1`
. Badges Maven Central/License, switcher i18n, RTL ar/ur

=== DOC-I18N-2 — README Plugin Internals (10 langues)
**Statut** : ✅ TERMINÉ (session 010)
. 10 fichiers `README.plugin.{en,zh,hi,es,fr,ar,bn,pt,ru,ur}.md`
. Architecture multi-module, patterns Tier 0, publication nmcp, runAllTests

'''
== EPIC PUB-5 — Verification

=== PUB-5.1 — Verifier Maven Central

| Artefact | URL |
|---|---|
| workspace-bom | https://central.sonatype.com/artifact/education.cccp/workspace-bom |
| agent-contracts | https://central.sonatype.com/artifact/education.cccp/agent-contracts |
| codebase-contracts | https://central.sonatype.com/artifact/education.cccp/codebase-contracts |
| vibecoding-contracts | https://central.sonatype.com/artifact/education.cccp/vibecoding-contracts |
| llm-pool-contracts | https://central.sonatype.com/artifact/education.cccp/llm-pool-contracts |
| pipeline-contracts | https://central.sonatype.com/artifact/education.cccp/pipeline-contracts |

'''
== EPIC MEM-3 : i18n-contracts — Contrats d'Internationalisation N0

*Découverte* : Session 111 bakery — L'i18n est une préoccupation transverse (bakery, slider, codex, training, magic-stick, document, readme). Mérite un artefact N0 partagé dans workspace-bom.

*Objectif* : Définir les contrats (interfaces + data classes) pour l'internationalisation — codes langue, catalogues, bundles de messages, résolution de locale. Zéro implémentation — chaque borough implémente selon son contexte (Thymeleaf pour bakery, Asciidoctor pour codex, Reveal.js pour slider, etc.).

*Charge* : ~8 pts (~2 sessions)
*Priorité* : P1 — Prérequis pour BKY-I18N et tout autre borough multilingue.
*Pattern* : Tier 0 (zéro dépendance externe, kotlin-stdlib only) — comme `codebase-contracts`, `llm-pool-contracts`, `pipeline-contracts`.

=== Langues supportées (10)

[cols="1,2,3,2,2"]
|===
| Rang | Code ISO 639-1 | Langue | Nom natif | RTL
| 1 | `en` | Anglais (English) | English | Non
| 2 | `zh` | Chinois Mandarin | 中文 | Non
| 3 | `hi` | Hindi | हिन्दी | Non
| 4 | `es` | Espagnol | Español | Non
| 5 | `fr` | Français | Français | Non
| 6 | `ar` | Arabe Standard | العربية | Oui
| 7 | `bn` | Bengali | বাংলা | Non
| 8 | `pt` | Portugais | Português | Non
| 9 | `ru` | Russe | Русский | Non
| 10 | `ur` | Ourdou | اردو | Oui
|===

=== Classes/Interfaces proposées

[cols="1,3,2,2"]
|===
| Classe | Description | Type | Pts

| `SupportedLanguage`
| Data class : `code` (ISO 639-1), `name` (English), `nativeName` (endonyme),
  `rtl` (Boolean), `localeTag` (ex: `"fr-FR"`, `"ar-SA"`).
| data class
| 1

| `LanguageCatalog`
| Object : liste statique des 10 `SupportedLanguage`, `all()` → List,
  `findByCode(code)` → SupportedLanguage?, `supportedCodes()` → Set<String>.
| object
| 1

| `I18nConfig`
| Data class : `activeLanguage` (String, default `"fr"`),
  `supportedLanguages` (List<String>, default `["fr"]`),
  `fallbackLanguage` (String, default `"en"`).
  `validate()` → Either<Error, Unit> (vérifie que active ∈ supported).
| data class
| 1

| `MessageBundle`
| Interface : `get(key: String, locale: Locale): String?`,
  `keys(): Set<String>`, `locale(): Locale`.
  Contrat pur — chaque borough fournit son implémentation
  (Properties pour Thymeleaf, YAML pour Asciidoctor, JSON pour Reveal.js).
| interface
| 1

| `LocaleResolver`
| Interface : `resolve(config: I18nConfig): Locale`.
  Contrat de résolution en cascade (CLI > properties > DSL > YAML > default).
  `resolveWithFallback(config): Locale` — si active ∉ supported → fallback.
| interface
| 1
|===

=== User Stories

[cols="1,4,2,2"]
|===
| US | Description | Pts | Priorité

| US-MEM-3.1
| **Structure projet** — Créer `i18n-contracts/` avec `build.gradle.kts`
  (pattern llm-pool-contracts, zéro dépendance), `settings.gradle.kts`
  autonome (nmcp), `gradle/libs.versions.toml`.
| 1
| 🔴 P0

| US-MEM-3.2
| **SupportedLanguage + LanguageCatalog** — TDD : data class 5 champs,
  object avec `all()`, `findByCode()`, `supportedCodes()`.
  8-10 tests unitaires (construction, lookup existant/inexistant,
  RTL arabe/ourdou, codes normalisés).
| 2
| 🔴 P0

| US-MEM-3.3
| **I18nConfig** — TDD : data class 3 champs + `validate()` retournant
  `Either<I18nError, Unit>`. 6-8 tests (config valide, active hors
  supported, fallback non supporté, defaults).
| 1.5
| 🟠 P1

| US-MEM-3.4
| **MessageBundle + LocaleResolver** — Interfaces pures (contrats).
  `MessageBundle` : 3 méthodes. `LocaleResolver` : 2 méthodes.
  Tests de contrat (mock implementations) — 4-6 tests.
| 1.5
| 🟠 P1

| US-MEM-3.5
| **Cucumber BDD** — Feature `i18n_contracts.feature` :
  résolution locale cascade, fallback si langue non supportée,
  lookup message par clé, catalogue 10 langues complet.
  4-6 scénarios.
| 1.5
| 🟡 P2

| US-MEM-3.6
| **Publication Maven Central** — nmcp publish,
  vérification https://central.sonatype.com/artifact/education.cccp/i18n-contracts.
  Mise à jour BOM racine.
| 0.5
| ✅ TERMINÉ (S-011 — 0.0.2 sur Central)
|===

=== Consommateurs prévus

[cols="2,3,4"]
|===
| Borough | Usage i18n | Implémentation
| Bakery | Templates Thymeleaf 36 fichiers | `messages_{code}.properties` + `#{key}`
| Slider | Slides Reveal.js | `i18n/{code}.json` + JS bundle
| Codex | PDF/EPUB Asciidoctor | Attributs localisés AsciiDoc
| Training | Formulaires Google Forms | Labels multilingues
| Magic-stick | Site documentation | `messages_{code}.properties` Thymeleaf
| Document | Génération AsciiDoc | Templates localisés
| Readme | README générés | Templates par langue
|===

=== Note architecture

Cet artefact est *strictement* un contrat (interfaces + data classes).
Aucune implémentation — pas de `ResourceBundle`, pas de `Properties.load`,
pas de parsing YAML/JSON. Chaque borough fournit sa propre implémentation
adaptée à son format de template (Thymeleaf, Asciidoctor, Reveal.js, etc.).

Le contrat garantit l'interopérabilité : un `I18nConfig` produit par bakery
peut être consommé par runner-gradle pour orchestrer un site multilingue.

'''
== EPIC MEM-4 : opencode-session-contracts — Protocole Thin Client opencode↔Gradle

*Découverte* : Session 103 codebase brainstorming (2026-06-14) — Architecture thin-client
opencode↔Gradle. opencode devient une UI terminal uniquement, toute la logique métier
(LLM, RAG, tools) est dans des tâches Gradle.

*Objectif* : Définir les contrats N0 pour le protocole de session entre un thin client
(opencode) et le moteur Gradle (codebase VibecodingTask). 7ème artefact N0 dans workspace-bom.

*Charge* : ~8 pts (~2 sessions)
*Priorité* : P1 — Dépend de EPIC V (VibecodingTask moteur) et EPIC X (verify→adapt) dans codebase.
*Pattern* : Tier 0 (zéro dépendance externe, kotlin-stdlib only).

=== Architecture

```
opencode (thin client UI)
    │ SessionPrompt (contrat N0)
    ▼
runner-gradle (N3) — pont thin-client ↔ Gradle engine
    │ VibecodingTask
    ▼
codebase-gradle (N1) — moteur LLM + RAG + tools
```

=== Classes/Interfaces proposées

[cols="1,3,2,2"]
|===
| Classe | Description | Type | Pts

| `SessionPrompt`
| Data class : `sessionId` (UUID), `prompt` (String), `context` (AgentContext?),
  `maxActions` (Int, default 10), `model` (String?, override modèle).
| data class
| 1

| `SessionResponse`
| Data class : `sessionId` (UUID), `output` (String formatée pour affichage),
  `toolCalls` (List<ToolCallRecord>), `tokenUsage` (TokenUsage),
  `status` (SessionStatus).
| data class
| 1

| `AgentContext`
| Data class : `eagerRules` (String, règles AGENT.adoc),
  `ragChunks` (List<String>, résultats RAG pgvector),
  `graphRelations` (String, relations Graphify),
  `backlogItems` (List<String>, items BACKLOG pertinents).
| data class
| 1

| `SessionStatus`
| Enum : `IN_PROGRESS`, `COMPLETED`, `ERROR`, `CANCELLED`.
| enum
| 0.5

| `ToolCallRecord`
| Data class : `toolName` (String), `args` (Map<String,String>),
  `result` (String), `timestamp` (Instant).
| data class
| 0.5

| `TokenUsage`
| Data class : `promptTokens` (Int), `completionTokens` (Int),
  `totalTokens` (Int), `cost` (Double?).
| data class
| 0.5
|===

=== User Stories

[cols="1,4,2,2"]
|===
| US | Description | Pts | Priorité

| US-MEM-4.1
| **Structure projet** — Créer `opencode-session-contracts/` avec
  `build.gradle.kts` (pattern llm-pool-contracts, zéro dépendance),
  `settings.gradle.kts` autonome (nmcp), `gradle/libs.versions.toml`.
| 1
| ✅ DONE

| US-MEM-4.2
| **SessionPrompt + SessionResponse + SessionStatus** — TDD : data classes
  + enum. 6-8 tests unitaires (construction, validation, statuts).
| 2
| ✅ DONE

| US-MEM-4.3
| **AgentContext + ToolCallRecord + TokenUsage** — TDD : data classes.
  4-6 tests unitaires (construction, sérialisation).
| 1.5
| ✅ DONE

| US-MEM-4.4
| **Cucumber BDD** — Feature `opencode_session.feature` :
  prompt→réponse cycle complet, session resume, erreur→status ERROR,
  contexte injecté programmatiquement.
  8 scénarios PASS.
| 1.5
| ✅ DONE

| US-MEM-4.5
| **Publication Maven Central** — nmcp publish,
  vérification https://central.sonatype.com/artifact/education.cccp/opencode-session-contracts.
  Mise à jour BOM racine.
| 0.5
| ✅ TERMINÉ (S-011 — 0.0.2 sur Central)
|===

=== Consommateurs prévus

[cols="2,3,4"]
|===
| Borough | Usage | Rôle
| Codebase (N1) | SessionProtocolTask — implémente le moteur contre le contrat | Producteur
| Runner (N3) | Pont thin-client ↔ Gradle — dispatch SessionPrompt → SessionProtocolTask | Consommateur
| Workspace (N4) | Provisioning — scaffold projet avec SessionProtocolTask câblé | Consommateur
|===

=== Note architecture

Cet artefact est *strictement* un contrat (data classes + enum).
Aucune implémentation — pas de logique LLM, pas de RAG, pas de tools.
Le contrat garantit l'interopérabilité : n'importe quel thin client
(opencode, CLI custom, IDE plugin) peut dialoguer avec le moteur Gradle
via ce format standardisé.

=== Référence

Brainstorming Session 103 codebase (2026-06-14) — Discussion architecture
thin-client opencode↔Gradle. Contrats N0 dans workspace-bom (MEMPHIS),
moteur dans codebase (N1), pont dans runner (N3).

'''
== EPIC SEC-1 : Traquer et fixer les appels LLM interdits dans les tests cross-boroughs

*Découverte* : Session 002 container-provision (2026-06-29) — `./gradlew build` sur
workspace-bom déclenche `check` → `runAllTests` → `./gradlew check` dans les 13 boroughs.
8 appels LLM au modèle DeepSeek ont été faits pendant la session, violant la règle
AGENTS.adoc : "Modèles autorisés : gpt-oss:120b-cloud, gemma4:31b-cloud" et
"Ports 11434-11436 INTERDITS".

*Objectif* : Identifier quels tests font des appels LLM réseau réels (pas mockés)
lors d'un `./gradlew check`, et les protéger avec `assumeTrue(isOllamaReady(port))`
ou les mocker pour qu'aucun appel réseau non autorisé ne parte.

*Contrainte* : `runAllTests` doit continuer à être lié à `check` du BOM — c'est
la safety net cross-boroughs. Ce sont les tests coupables qui doivent être fixés.

*Charge* : ~6 pts (~1-2 sessions)
*Priorité* : 🔴 P0 — Sécurité/Conformité
*Statut EPIC* : ✅ TERMINÉ (session 013) — audit statique + dynamique, 1 borough coupable corrigé

=== US-SEC-1.1 — Audit des tests cross-boroughs
**Pts** : 2
**Statut** : ✅ TERMINÉ (session 013)
*Méthode* : Audit statique (scan src/test + src/main) + vérification dynamique (analyse
des GradleRunner qui exécutent des tâches LLM, vérification des mocks/assumeTrue/WireMock).
*Origine des 8 appels DeepSeek* : les defaults du code `main` des plugins (`gpt-oss:120b-cloud`,
`localhost:11434`), déclenchés par les tests fonctionnels GradleRunner qui exécutent les
tâches Gradle sans mocker. La purge session 012 a corrigé les defaults → `gpt-oss`/`gemma4`
+ ports 11437-11465. Mais les tests continuaient à faire des appels réseau (vers les
nouveaux defaults autorisés) — coupable = planner-gradle.

*Résultat* :
- **planner-gradle** — COUPABLE. `DecomposeIntentionPluginTest` (2 tests) +
  `DecomposeIntentionMultiCanalTest` (4 tests) exécutent `generatePlan` via GradleRunner
  sans mock. `IntentionPlanner.plan()` appelle `model.chat()` (appel réseau réel).
  *Corrigé* : ajout `-PollamaBaseUrl=http://localhost:1` (port fermé) → l'appel échoue
  immédiatement sans toucher Ollama réel. Les tests acceptaient déjà `[ERROR]`/`[FATAL]`.
- **plantuml-gradle** — PROPRE. Tests `generatePlantumlDiagrams` utilisent WireMock ou
  `-Dplantuml.test.mode=true` (mode simulé, pas d'appel réseau). `@Tag("real-llm")` +
  `assumeTrue` pour les tests réels. 1 test `@Ignore` (nécessite Ollama réel).
- **bakery-gradle** — PROPRE. `generateSite` n'appelle pas le LLM. `generateSiteFromIntention`
  utilise `FakeLlmService` en test. Cucumber → `FakeLlmService` systématique.
- **codebase-gradle** — PROPRE. `CodebasePluginTest` exécute seulement `tasks`. Appels LLM
  réels via `OllamaLlmProviderTest` protégés par `assumeTrue(isOllamaReady(ciPort))`.
- **slider-gradle** — PROPRE. Scénario `@integration` (Ollama réel) exclu via
  `FILTER_TAGS = "not @wip and not @integration"`. Tests Cucumber utilisent `startMockLlm`.
- **codex, dashboard, graphify, hyperframes, jhipster, readme, capsule, api-key-pool** —
  PROPRE. Aucun GradleRunner qui exécute une tâche LLM sans mock.

*Alertes corrigées* (ports interdits 11434-11436 résiduels) :
- plantuml `ollama-local-smollm-135.yml` (test resource) → `11440`
- plantuml `application-test.yaml` (test resource) → `11440`
- plantuml `application.yaml` (main resource) → `11438`

=== US-SEC-1.2 — Mocker ou skipper les tests coupables
**Pts** : 3
**Statut** : ✅ TERMINÉ (session 013)
- 6 tests planner-gradle coupables corrigés : `-PollamaBaseUrl=http://localhost:1` injecté
  dans `DecomposeIntentionPluginTest` (2) + `DecomposeIntentionMultiCanalTest` (4).
  L'appel réseau échoue immédiatement (Connection refused sur port 1) sans toucher Ollama.
  Les assertions acceptaient déjà `[ERROR]`/`[FATAL]` — pas de changement de logique de test.

=== US-SEC-1.3 — CI guard : vérification automatique
**Pts** : 1
**Statut** : ✅ TERMINÉ (session 013)
- Audit dynamique confirme qu'aucun test ne fait d'appel LLM réseau non protégé.
  Les tests restants qui font des appels réels sont derrière `assumeTrue` ou `@Tag`.
  Recommandation : ne pas ajouter de guard supplémentaire — l'audit statique + dynamique
  suffit. `runAllTests` peut être lancé sans risque d'appel LLM non autorisé.

'''
== EPIC CI-1 : CI/CD runAllTests cross-boroughs GitHub Actions

*Découverte* : Session 014 (2026-06-30) — workspace-bom n'avait aucune CI/CD.
`runAllTests` (13 boroughs en `./gradlew check`) n'était exécutable qu'en local.
Workflow `.github/workflows/test.yml` créé et poussé. 1er run #28412781846 :
10/13 OK, 3 échecs diagnostiqués.

*Objectif* : Rendre `runAllTests` lançable depuis GitHub Actions + nourrir le
besoin LLM des tests d'intégration avec `qwen3:0.6b` (exception CI AGENTS.adoc).

*Charge* : ~4 pts (~1 session)
*Priorité* : 🔴 P0
*Statut EPIC* : 🔄 EN COURS (session 014 — workflow créé, 3 échecs à fixer session 015)

=== US-CI-1.1 — Workflow test.yml créé
**Pts** : 1
**Statut** : ✅ TERMINÉ (session 014)
- `.github/workflows/test.yml` : checkout workspace-bom + 13 siblings + JDK 24 + runAllTests.
- Run #28412781846 déclenché, 10/13 boroughs OK.

=== US-CI-1.2 — Fix api-key-pool-gradle wrapper jar
**Pts** : 0.5
**Statut** : 🟡 À FAIRE (re-confirmé S-028 — wrapper jar toujours gitignoré, `git check-ignore` positif)
- `gradle-wrapper.jar` gitignoré dans api-key-pool-gradle (`.gitignore` ligne 23).
- Fix : retirer la ligne `.gitignore` + committer le jar, OU step CI `gradle wrapper`.
- *Ordre suggéré S-029* : après ROLLOUT-1 (document), 0.5 pt — quick win.

=== US-CI-1.3 — Fix readme-gradle + slider-gradle (check not found)
**Pts** : 1
**Statut** : ✅ PARTIELLEMENT RÉSOLU (S-026) — readme-gradle confirmé racine sans check
(consommateur, pattern article 0124) ; retiré de `runAllTests` avec les autres
racines consommateurs (capsule/codebase/codex/graphify/hyperframes). slider-gradle
a un check vert, reste dans la liste. La solution `List<Pair<String,String?>>`
reste pertinente si on veut tester les SOUS-RÉPERTOIRES plugin.

=== US-CI-1.4 — Ollama qwen3:0.6b en CI
**Pts** : 1
**Statut** : 🟡 À FAIRE (re-confirmé S-028 — pas de step Ollama dans test.yml)
- Step CI : `docker run -d -p 11466:11467 ollama/ollama` + `ollama pull qwen3:0.6b`.
- Env vars : `OLLAMA_TEST_PORT=11466`, `OLLAMA_TEST_MODEL=qwen3:0.6b`.
- Tests `assumeTrue(isOllamaReady(port))` se débloquent. Pas de secret requis.
- *Dépend de* : US-CI-1.2 (wrapper) — même workflow CI à durcir.

=== US-CI-1.5 — Secrets workspace-bom
**Pts** : 0.5
**Statut** : ✅ TERMINÉ (session 014)
- `GITHUB_TOKEN` natif suffit pour cloner les 13 repos publics.
- Aucun secret additionnel requis pour `runAllTests`.
- Pour publication Maven Central (futur) : GPG key + Maven Central credentials.

'''
== ✅ EPIC RUNTIME-CONTRACTS — 8e artefact N0 (session 022 — TERMINÉ)

_Source_ : `CADRAGE_EPIC_RUNTIME_AGENTIQUE.adoc` (jardin secret).
_Constat_ : aucun des 7 artefacts N0 existants ne formalise le bootstrapping
d'une session formation agentique. `opencode-session-contracts` décrit une
session _dev opencode_, pas une session _stagiaire non-dev_.

=== US réalisées (7/7)

[cols="1,3,2,2"]
|===
| US | Description | Pts | Statut

| RUNTIME-CONTRACTS-0
| *Cadrage EPIC* — audit 7 artefacts, constats, décisions, découpage US
| 1
| ✅ TERMINÉ (cadrage 2026-08-12)

| RUNTIME-CONTRACTS-1
| *SessionBootstrap + ByokLlmConfig* — data class immutable + LlmProviderKind enum. Invariants OLLAMA_LOCAL=baseUrl, GEMINI/HUGGINGFACE/CUSTOM=apiKeyEnvVar. 13 unit
| 2
| ✅ TERMINÉ (S-022)

| RUNTIME-CONTRACTS-2
| *ToolExposure* simplifié — guardrail foundry/public. Data class + 2 unit
| 1
| ✅ TERMINÉ (S-022)

| RUNTIME-CONTRACTS-3
| *SessionMemoryContract + LearnerProfile* — interface + data class 9 champs. 8 unit
| 2
| ✅ TERMINÉ (S-022)

| RUNTIME-CONTRACTS-4
| *InteractionProtocol + FormationTurn* — interface + data class consommant opencode-session-contracts. 5 unit
| 2
| ✅ TERMINÉ (S-022)

| RUNTIME-CONTRACTS-5
| *FormationRuntimePort + SessionId + SessionSummary* — port public OSS + data classes. 5 unit
| 2
| ✅ TERMINÉ (S-022)

| RUNTIME-CONTRACTS-6
| *MaterialUpdateContract + MaterialUpdateResolver* — data class + sealed result + interface. 10 unit
| 2
| ✅ TERMINÉ (S-022)

| RUNTIME-CONTRACTS-7
| *Publication Maven Central* — runtime-contracts:0.0.1 (bundle `04af4f31`) + BOM 0.0.24 (bundle `d3b82434`)
| 1
| ✅ TERMINÉ (S-022)
|===

_Total_ : 8 US, ~14 pts. _Méthode_ : baby-step DDD/TDD/BDD 3 niveaux.
_Tests_ : 55 tests 0 FAIL (43 unit + 11 cucumber + 1 background).
_Fichiers_ : 13 prod + 7 test + 1 feature.
_Publications_ : runtime-contracts 0.0.1 + BOM 0.0.24 sur Maven Central.
_Débloque_ : codex CDX-RC-04 (pont RAG SessionMemoryContract).

'''
== ✅ EPIC MEM-CATALOG — Version Catalog publié : le BOM comme unique source de vérité des versions plugins (session 027 — TERMINÉ)

_Cadré Session 025 (2026-09-01) — `.agents/CADRAGe_EPIC_MEM_VERSION_CATALOG.adoc`._
_Origine : demande pilote (session 243 document-gradle) — centraliser les versions plugins dans
le BOM, transportées par artefact Maven (pas de référence physique au repo BOM)._
_**TERMINÉ S-026 (CAT-1/2/3) + S-027 (CAT-4/5 + publication Central 0.0.29) — 5/5 US ~21/21 pts.**_

[CAUTION]
====
**Motivation** : les versions des plugins `education.cccp:*` sont dupliquées chez chaque
consommateur (hardcode ou toml local) → bumps éparpillés récurrents (S-241, S-209, S-120,
EPIC MEMPHIS-BOM-FIX) + classe de bug hygiène version (`BakeryPluginPublicationTest`).
Constat déclencheur : MEMPHIS a déjà 3 sources de version divergentes en lui-même
(toml 0.0.26 / platform 0.0.29 / Central 0.0.27) et un split-brain `agent-contracts`
(toml 0.0.2 vs platform 0.0.3). Le toml référence déjà les 12 plugins — la source de
vérité existe, elle n'est pas transportée.

**Solution** : 2 mécanismes Gradle natifs complémentaires — (1) platform BOM (existant)
pour les *dépendances* sans version ; (2) *version catalog publié* (`workspace-catalog`)
consommé via `versionCatalogs { create("ws") { from("education.cccp:workspace-catalog:X") } }`
dans settings — accesseurs typés `ws.versions.*` pour la version propre du plugin +
les plugins appliqués (`alias(ws.plugins.bakery)`). Pin unique par borough (1 ligne),
adoption paresseuse, zéro couplage physique au repo BOM (Maven only).
====

=== User Stories — baby steps DDD/TDD/BDD

[cols="1,4,2,2,2"]
|===
| US | Description | Tests | Pts | Dépend de

| MEM-CAT-1
| ✅ TERMINÉ (S-026)
| *Suppression du split-brain intra-MEMPHIS* — platform consomme le toml
  (`api(libs.*)` au lieu de hardcoder), toml aligné sur les valeurs réelles
  (agent-contracts 0.0.3, epub3 2.2.0 réelle — 3.0.1 fantôme corrigé, version BOM dérivée). POM avant/après
  identique (diff vide 77/77).
| UT alignement toml↔platform (5) + POM comparé
| 3 ✅
| —

| MEM-CAT-2
| ✅ TERMINÉ (S-026)
| *Publication `workspace-catalog`* — plugin `version-catalog` + publication
  `education.cccp:workspace-catalog:0.0.29` (`from(components["versionCatalog"])`,
  API Gradle 9) + POM complet + UT structurels. Capsule fantôme retiré (D8).
  `publishToMavenLocal` VERT (toml+pom+module signés).
| UT : 4 structure catalog (WorkspaceCatalogStructureTest)
| 5 ✅
| CAT-1

| MEM-CAT-3
| ✅ TERMINÉ (S-026)
| *Pilote consommateur bakery* — settings `create("ws") { from(...) }`,
  `version = ws.versions.bakery.plugin.get()`, `document-plugin` via
  platform sans version, test hygiène D3 migré. Gates : test 1665/3 (=baseline),
  functionalTest VERT, cucumber 7 (=baseline), mavenLocal VERT.
| FT mavenLocal + check bakery non-régression + PublicationTest migré
| 5 ✅
| CAT-2

| MEM-CAT-4
| ✅ TERMINÉ (S-027)
| *Preuve de propagation* — bump planner-plugin 0.0.1→0.0.2 (toml aligné
  Central S-074) → publication → consommateur voit la nouvelle version sans
  éditer son build (FT jetable + consommateur Gradle réel, pin inchangé).
  Protocole de release documenté `.agents/RELEASE_PROTOCOL.adoc`.
| FT propagation mavenLocal + Central + doc protocole
| 3 ✅
| CAT-3

| MEM-CAT-5
| ✅ TERMINÉ (S-027)
| *Cucumber BDD + doc migration* — `@version-catalog` (12 plugins résolvable /
  accessor typé / hygiène pas-de-hardcode, 3 scénarios PASSED) + README
  migration boroughs EN/FR. Généralisation borough par borough HORS EPIC.
| BDD : 3 scénarios + runner dédié (versionCatalogCucumberTest)
| 5 ✅
| CAT-4
|===

_Total_ : ~21 pts, 5 US, **TERMINÉ** (S-026 + S-027). Publication Central
0.0.29 : BOM + workspace-catalog, un seul bundle `e6f571c2` (AUTOMATIC),
vérifiés repo1 HTTP 200 — planner-plugin 0.0.2 propagé Central.

=== Décisions clés (détail dans le cadrage)

- D1 : 2 artefacts MEMPHIS (`workspace-bom` platform + `workspace-catalog` catalog), même release.
- D2 : périmètre = plugins education.cccp + versions cross-borough uniquement (déps tierces restent locales).
- D3 : la version *propre* d'un plugin reste dans son repo — le BOM câble ENTRE, pas DEDANS.
- D4 : pin unique `from(...)` par borough, jamais de version dynamique (reproductibilité).
- D5 : adoption paresseuse, borough par borough, jamais big bang.
- D8 : entrée catalog créée seulement à la 1re publication Central (pas de fantôme).
- D10 : le fix intra-MEMPHIS précède la publication (l'exemple doit être clean).
- D12 : transmission Maven only (Central + mavenLocal), zéro includeBuild/chemin FS.

=== Risques (détail dans le cadrage)

R-VC-1 couplage ordonnancement (polling Central) · R-VC-2 entrées fantômes · R-VC-3 collisions
`ws`/`libs` · R-VC-4 conventions includeBuild HORS SCOPE · R-VC-5 hardcodes `id(...) version`
restants · R-VC-6 sessions concurrentes (bakery, MEMPHIS epub3 WIP absorbé par CAT-1).

=== Projection sessions

* *S-026* — CAT-1 (3 pts, alignement toml↔platform)
* *S-027* — CAT-2 (5 pts, publication workspace-catalog)
* *S-028* — CAT-3 (5 pts, pilote bakery — coordination session bakery active)
* *S-028* — CAT-4 + CAT-5 (8 pts, propagation + BDD, clôture EPIC)
